[pytest]
testpaths = tests
markers =
    unit: fast unit tests
    integration: module integration tests
    e2e: end-to-end pipeline tests
    contracts: output and schema contract tests
    critics: critics-related tests
    slow: long-running tests
